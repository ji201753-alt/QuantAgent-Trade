Mission

This repository is no longer a simple fork or experimental trading bot.

It has evolved into a standalone institutional-grade quantitative market intelligence workstation composed of:

a realtime ingestion and streaming core,
probabilistic forecasting systems,
structural cognition systems,
replay and forensic investigation infrastructure,
contextual reasoning and chronology systems,
macro and contagion intelligence layers,
an immersive React operational workstation,
and runtime orchestration infrastructure.

The primary objective is NOT automated trading execution.

The objective is:

realtime market intelligence,
probabilistic operational awareness,
replay-driven forensic analysis,
structural market investigation,
cross-market monitoring,
contextual decision support,
arbitrage monitoring,
prediction market analysis,
and institutional-grade investigative workflows.

The repository must now be treated as a NEW PLATFORM.

Do NOT treat this codebase as a lightweight fork. Do NOT optimize for preserving legacy fork architecture. Do NOT attempt to preserve obsolete abstractions if they conflict with platform stability.

However:

Do NOT destroy or rewrite stable systems unnecessarily.

The project already contains substantial architecture and operational logic built during many iterative development phases.

The goal is now:

stabilization,
consolidation,
runtime correctness,
architectural coherence,
UX continuity,
performance hardening,
replay determinism,
operational consistency,
and production readiness.

The mission is NOT feature expansion.

No large new systems should be introduced unless strictly necessary for stabilization.

Current Platform State

The repository already contains:

async EventBus architecture,
modular connectors,
analytics pipelines,
forecasting systems,
TimesFM integration scaffolding,
Kronos structural cognition integration scaffolding,
probabilistic signal systems,
contextual intelligence systems,
chronology reconstruction,
replay systems,
investigation persistence,
runtime orchestration,
Zustand synchronization,
websocket streaming,
lightweight-charts integration,
cinematic React workstation UX,
arbitrage intelligence surfaces,
macro cognition systems,
reasoning and assistant grounding layers,
and extensive frontend operational tooling.

However, because development was performed incrementally through many AI-generated iterations, the repository may contain:

duplicated logic,
stale abstractions,
partial migrations,
inconsistent architecture,
placeholder integrations,
incomplete runtime handling,
conflicting state flows,
replay desynchronization,
frontend rendering instability,
weak connector recovery logic,
brittle async orchestration,
orphaned services,
dead files,
partial refactors,
and systems that appear operational superficially but fail under real execution.

Your responsibility is to identify and resolve these issues.

High-Level Objective

Your job is to transform this repository from a partially stabilized AI-generated system into a coherent production-grade operational intelligence platform.

You must:

audit the entire repository,
understand architecture before modifying it,
identify broken assumptions,
detect incomplete integrations,
consolidate duplicated systems,
harden runtime behavior,
improve deterministic replay consistency,
validate websocket synchronization,
stabilize frontend rendering,
validate TimesFM runtime behavior,
validate Kronos orchestration behavior,
improve operational continuity,
and leave the repository in a maintainable production-ready state.

This is NOT a code-generation task.

This is a platform stabilization and production-hardening task.

Critical Development Constraints
DO NOT DESTROY EXISTING SYSTEMS

Do not perform broad rewrites unless absolutely necessary.

Before changing architecture:

inspect existing flows,
inspect state dependencies,
inspect replay synchronization,
inspect frontend coupling,
inspect websocket propagation,
inspect EventBus flows,
and understand operational intent.

Never remove systems simply because they appear complex.

Many systems are interconnected.

You must first determine:

whether a system is operational,
partially operational,
obsolete,
duplicated,
or broken.

Only then may you refactor.

DO NOT INTRODUCE LARGE NEW SYSTEMS

Do not expand scope.

Avoid introducing:

new cognitive layers,
new intelligence engines,
new orchestration paradigms,
new frontend frameworks,
or new runtime architectures.

The repository already contains the required conceptual systems.

The current priority is:

consolidation,
stabilization,
orchestration,
correctness,
performance,
operational UX,
and deployment readiness.
UNDERSTAND THE PROJECT BEFORE MODIFYING IT

Before making substantial modifications:

map the backend architecture,
map the frontend architecture,
map EventBus flows,
map runtime initialization,
map replay synchronization,
map websocket propagation,
map Zustand synchronization,
map forecasting overlays,
map Kronos integration points,
map TimesFM integration points,
and map investigation persistence.

You are expected to reason about the platform globally.

Do not make isolated local fixes that create systemic regressions.

Expected Technical Priorities
Priority 1 — Runtime Stability

The workstation must boot reliably.

Audit:

startup sequencing,
async initialization,
connector boot order,
runtime orchestration,
websocket lifecycle,
frontend hydration,
replay restoration,
persistence loading,
model loading,
and graceful degradation behavior.

Fix:

race conditions,
deadlocks,
reconnect failures,
stale websocket state,
async leaks,
memory leaks,
event storms,
and duplicated subscriptions.
Priority 2 — EventBus and Synchronization Integrity

The EventBus is the backbone of the platform.

Audit:

event propagation,
listener cleanup,
fanout stability,
replay synchronization,
frontend subscription handling,
chronology reconstruction,
and cross-workspace synchronization.

Ensure:

deterministic replay,
synchronized overlays,
consistent chronology state,
and frontend coherence under high-frequency updates.
Priority 3 — Frontend Operational Integrity

The frontend is intended to behave like an institutional intelligence workstation.

Audit:

App.tsx orchestration,
panel composition,
overlay rendering,
chart orchestration,
replay synchronization,
contextual focus propagation,
runtime telemetry,
and Zustand state integrity.

Fix:

unnecessary rerenders,
broken panel interactions,
stale hover synchronization,
overlay conflicts,
inconsistent motion systems,
visual desynchronization,
and workspace instability.

Preserve:

cinematic UX,
layered intelligence rendering,
dense operational layout,
and realtime situational awareness.
Priority 4 — TimesFM Integration Validation

TimesFM must not be placeholder-only.

Audit whether:

model loading actually works,
runtime orchestration exists,
forecasts are synchronized,
overlays render correctly,
replay synchronization is deterministic,
and fallback behavior is implemented correctly.

Ensure:

lazy loading,
runtime telemetry,
CPU/GPU fallback,
forecast confidence rendering,
and overlay continuity.

The integration should support real local execution.

Priority 5 — Kronos Structural Cognition Validation

Kronos must behave as an additive structural cognition layer.

Audit whether Kronos:

replaced prior overlays,
broke replay synchronization,
degraded chart orchestration,
duplicated state logic,
or destabilized investigations.

Ensure Kronos:

integrates with replay,
integrates with investigations,
integrates with overlays,
integrates with chronology,
and behaves coherently with TimesFM and contextual intelligence.
Priority 6 — Investigation and Replay Continuity

Replay and investigations are core operational features.

Audit:

replay determinism,
timeline reconstruction,
chronology coherence,
overlay replay synchronization,
assistant grounding continuity,
investigation persistence,
and contextual anchoring.

Ensure operators can:

replay events,
inspect overlays historically,
navigate evidence chains,
restore investigations,
and move through chronology coherently.
Priority 7 — Repository Cleanup

The repository likely contains:

dead code,
stale files,
experimental leftovers,
duplicate utilities,
conflicting services,
and abandoned architecture.

Perform careful cleanup.

But:

Never remove systems before validating dependencies.

Many apparently redundant systems may still be operationally connected.

Frontend Philosophy

The frontend is NOT a generic dashboard.

It is an immersive operational intelligence environment.

The interface should:

feel dense,
feel realtime,
feel synchronized,
feel operational,
feel investigative,
and feel continuity-driven.

Avoid:

oversimplification,
consumer-dashboard aesthetics,
excessive whitespace,
generic admin layouts,
and disconnected panels.

Preserve:

cinematic UX,
layered intelligence overlays,
operational telemetry,
synchronized workspaces,
and contextual continuity.
Backend Philosophy

The backend is NOT a traditional monolithic trading bot.

It is:

async-first,
event-driven,
replay-aware,
probabilistic,
investigation-oriented,
and operationally observable.

Preserve:

modularity,
deterministic replay,
typed schemas,
and realtime synchronization.

Avoid:

hidden state,
tightly coupled orchestration,
implicit side effects,
and blocking operations.
Deployment Philosophy

The workstation is intended to run locally as a professional operational environment.

The repository should support:

local development,
local operational monitoring,
TimesFM local execution,
Kronos local indexing,
websocket streaming,
replay persistence,
and frontend/backend orchestration.

Deployment flows must become:

deterministic,
understandable,
reproducible,
and debuggable.
Expected Final Outcome

The final platform should:

boot reliably,
stream reliably,
replay deterministically,
synchronize overlays correctly,
maintain frontend coherence,
expose runtime diagnostics,
preserve inves